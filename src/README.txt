main.py		is the main file corresponding to the model in crf_model.model
train_hyperparameters		is the file where it is performed the grid search
converting_crowd.py 		convert the crowd-sourced-annotations.txt in the standard format saved in crowd-converted.txt